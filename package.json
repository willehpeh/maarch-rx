{
  "name": "maarch-training-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "concurrently \"mongod --config /usr/local/etc/mongod.conf\" \"nodemon index\" \"cd frontend && ng serve\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.6",
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.5.10",
    "mongoose-unique-validator": "^2.0.3"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
